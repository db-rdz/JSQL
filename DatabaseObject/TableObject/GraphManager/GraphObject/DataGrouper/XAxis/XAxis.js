export default class XAxis {
    constructor({ xAxisData = {} }) {
        this.xAxisData = xAxisData;
    }

    getAxisData() {
        return this.xAxisData;
    }

    getUniqueValues() {
        
    }
}